# Strings are immuable
a = "Samriddh"
print(a.upper())
print(a.lower())

# rstrip() -> it removes any trailing characters.

a= "Hello !!!!"
print(a.rstrip("!"))

#replace -> it raplaces all occurences of a string with another one.
a = "hello"
print(a.replace("hello", "hiiii"))

#split -> this method splits the stirng at the specified instance and returns the separated strings a list item.
a = "hello samriddh how are you"
print(a.split())

# capitalize() , count(), endswith(), find(), index()